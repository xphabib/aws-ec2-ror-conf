#RVMDIR="$( dirname -- "$( readlink -f -- "$0"; )"; )/rvm.sh"
#sh $RVMDIR 3.0.0

# json='{"access_token":"kjdshfsd", "key2":"value"}' 
# echo $json | grep -o '"access_token":"[^"]*' | grep -o '[^"]*$'

# databasename=`cat config.json | json select '.uri'`
# $ echo $databasename

which nvm